# =============
# == SOURCES ==
# =============

set(SOURCES
  src/main.cpp
)

# ===========
# == BUILD ==
# ===========

# Create target
add_executable(bofstd_tool
  ${SOURCES}
)

# Includes
target_include_directories(bofstd_tool
  PRIVATE
    $<BUILD_INTERFACE:${CMAKE_CURRENT_SOURCE_DIR}/src/>
)

# Link
target_link_libraries(bofstd_tool
  PRIVATE
    ONBINGS::bofstd
#    Qt5::Core
#    Qt5::Gui
#    Qt5::Widgets
)

# Defines
target_compile_definitions(bofstd_tool
  PRIVATE
)

# Properties
#set_target_properties(bofstd_tool
#  PROPERTIES 
#    AUTOMOC ON
#    AUTORCC ON
#)   

# =============
# == INSTALL ==
# =============

if(NOT APPLE)

  # Fix RPATH upon installation by replacing
  # the absolute RPATH generated by the compiler
  # to where libraries were really installed
  set_target_properties(bofstd_tool PROPERTIES
    INSTALL_RPATH ${RELATIVE_PATH_BIN_TO_LIB}
  )

endif()

# Install the target runtime
install(
  TARGETS  bofstd_tool
  EXPORT   bofstd_tool
  RUNTIME  DESTINATION ${CMAKE_INSTALL_BINDIR}     # For binaries and dll
           COMPONENT   bofstd-tool
)

